<resources>

    <style name="Widget.App.Button" parent="Widget.MaterialComponents.Button">
        <!--
            此处的 name 为 Button Attributes ，
            数值为对应的资源（类型可以为：Attribute,Boolean,Color,Dimension,Drawable,Float,Font,Fraction,String）
        -->
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:drawablePadding">11dp</item>
        <item name="android:layout_width">150dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <style name="Widget.App.RadioButton" parent="Widget.AppCompat.CompoundButton.RadioButton">
        <!--
            此处的 name 为 RadioButton 的 Attributes ，
            数值为对应的资源（类型可以为：Attribute,Boolean,Color,Dimension,Drawable,Float,Font,Fraction,String）
        -->
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:drawablePadding">5dp</item>
        <item name="android:buttonTint">?attr/colorPrimary</item>
    </style>

    <style name="Widget.App.TextView" parent="Widget.MaterialComponents.TextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">15dp</item>
        <item name="android:textSize">20sp</item>
    </style>
</resources>